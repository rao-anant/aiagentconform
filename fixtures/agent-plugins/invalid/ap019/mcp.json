{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "invalid": {
      "type": "stdio",
      "command": "node",
      "cwd": "${PLUGIN_ROOT}/../escape"
    }
  }
}
